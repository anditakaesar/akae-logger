export const FORMATS = {
  DATE_FORMAT: 'yyyyMMdd',
  TIME_FORMAT: 'hh:mm:ss.SSS',
}

export const ROUTE = {
  API: {
    record: '/api/record'
  }
}

export const CST = {
  FORMATS,
  ROUTE,
}

export default CST